This is for repo test.

## How to run

This is a static site (HTML, CSS, and JavaScript). No build step is required.

1. Open `index.html` in your browser, or serve the folder locally:

   ```bash
   python3 -m http.server 8000
   ```

2. Visit [http://localhost:8000](http://localhost:8000).
